<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.eleicao_pesquisa">

    <!-- Permissões (fora de <application>) -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:label="eleicao_pesquisa"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="true"
        android:theme="@style/LaunchTheme">

        <!-- Receiver que será acionado pelo AlarmManager (notificação diária) -->
        <receiver
            android:name="com.example.eleicao_pesquisa.receivers.DailyAlarmReceiver"
            android:exported="true" />

        <!-- Receiver para reagendar alarmes/notificações após boot -->
        <receiver
            android:name="com.example.eleicao_pesquisa.receivers.BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- MainActivity do Flutter (android:exported obrigatório para Android 12+) -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>
</manifest>
